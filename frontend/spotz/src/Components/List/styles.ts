import { Theme } from '@mui/material/styles';

export const formControl = (theme: Theme) => ({
  margin: theme.spacing(1),
  minWidth: 120,
  marginBottom: '30px',
});

export const selectEmpty = (theme: Theme) => ({
  marginTop: theme.spacing(2),
});
